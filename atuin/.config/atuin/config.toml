# ── Atuin Configuration ───────────────────────────────────────────────────────
# Local SQLite shell history with fuzzy search
search_mode = "fuzzy"
filter_mode = "global"
style = "compact"
inline_height = 20
show_preview = true
enable_auto_sync = false
update_check = false
keymap_mode = "vim-insert"
