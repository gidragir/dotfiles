# ── Default Programs / Session Environment ───────────────────────────────────
# Executed for login shells (GUI session startup environment / login SSH)
export EDITOR='nvim'
export VISUAL='nvim'
export PAGER='less'
